const _ = require('lodash');

const cluster = {
    get: () => {
        return {
            id:"ID_GOES_HERE"
        };
    }
}

const applications = {
    get: () => {

    }
}

module.exports = {cluster};
